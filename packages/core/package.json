{
  "name": "@rivet/core",
  "version": "0.1.0",
  "description": "Core orchestration engine for RIVET analysis",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs",
      "require": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts --tsconfig ../../tsconfig.tsup.json --clean --external @rivet/parsers",
    "dev": "tsup src/index.ts --format cjs,esm --dts --tsconfig ../../tsconfig.tsup.json --watch --external @rivet/parsers",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@rivet/parsers": "workspace:*",
    "glob": "^11.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "tsup": "^8.0.1",
    "vitest": "^4.1.11"
  },
  "keywords": [
    "code-quality",
    "analysis",
    "linting"
  ]
}
