/**
 * Re-exported from `@rivet/core` so the web app can read the model defaults
 * without pulling LangChain into its bundle.
 */
export {
  DEFAULT_EXPLAIN_MODEL,
  DEFAULT_OPENAI_MODEL,
  resolveModel,
} from '@rivet/core'
